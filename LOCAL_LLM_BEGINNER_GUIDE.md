# OpenCode + ローカルLLM 入門ガイド（初心者向け）

## 1. まず結論（最短ルート）

初心者の方は、次の順で進めると失敗しにくいです。

1. **OpenCode をインストール**
2. **Ollama をインストール**（ローカルでLLMを動かす定番）
3. **軽めの日本語対応モデルを1つだけ入れる**
4. **CLIで対話テスト**
5. **Webアプリ（チャット + 謎解きロジック）に接続**

---

## 2. あなたの作りたいもの（マーダーミステリー）に必要な構成

あなたのアプリは、ざっくり次の4層に分けると作りやすいです。

- **UI層（Web画面）**
  - プレイヤーがAIと会話する画面
- **ゲーム管理層（サーバー）**
  - シナリオ進行、フラグ管理、制限時間、ヒント制御
- **AI対話層（プロンプト制御）**
  - 「犯人情報は終盤まで出さない」などのルールを強制
- **LLM実行層（ローカルLLM）**
  - Ollama + Gemma系などで推論実行

ポイントは、**ゲームの正解管理をLLMに丸投げしないこと**です。  
正解判定・進行フラグは必ずサーバー側で管理してください。

---

## 3. 最初の開発スタック（初心者向け推奨）

- フロントエンド: HTML/CSS/JavaScript（または Next.js）
- バックエンド: Node.js + Express
- ローカルLLM実行: Ollama
- モデル例:
  - `gemma3` / `gemma2` 系（環境に合わせて）
  - 日本語品質を重視するなら日本語チューニングモデルも検討

> 注意: モデル名や対応状況は更新されるため、インストール時に最新を確認してください。

---

## 4. 実装の最小ステップ

### Step A: ローカルLLMの疎通確認

- まずCLIで1問1答が成立することを確認。
- ここで詰まるとWeb統合も詰まるため最優先。

### Step B: サーバーAPIを1本作る

- `/api/chat` に `message` をPOST
- サーバーがローカルLLMへ問い合わせ
- 応答テキストを返す

### Step C: 謎解きの進行管理を入れる

- `state`（例: `intro`, `investigation`, `final`）
- 状態ごとにAIへ渡すシステム指示を切り替える

### Step D: 禁則ルールを作る

例:
- 序盤は犯人名を出さない
- 証拠が揃うまで決定的ヒントを出さない
- ユーザーが脱線しても世界観を維持する

---

## 5. 失敗しにくいプロンプト設計

システムプロンプトに以下を明示します。

- あなたの役割（登場人物 / ナレーター）
- 開示可能な情報の範囲
- 禁止事項（ネタバレ条件）
- 出力形式（短く・箇条書き等）

さらに、サーバー側で「回答後チェック」を行い、禁則違反を検出したら再生成すると安定します。

---

## 6. 性能面（ローカル運用で重要）

- まずは **小〜中サイズモデル** で開始
- 会話履歴は毎回全部渡さず、要約メモを使う
- 長文より、短いターンで進行

推論が遅い場合:
- モデルを軽量化
- 同時実行数を減らす
- 文章量を制限する

---

## 7. 初学者向けロードマップ（2〜4週間）

### 1週目
- OpenCode と Ollama の導入
- CLIチャット疎通
- GitHubでプロジェクト管理開始

### 2週目
- Web画面 + `/api/chat` の接続
- 単純な会話がブラウザで動く状態に

### 3週目
- シナリオ状態管理
- ネタバレ防止ルール
- ログ保存

### 4週目
- テストプレイ
- ヒントの難易度調整
- エラー処理・UI改善

---

## 8. あなた向けの次アクション

1. 「まず動く最小版」を作る（1シーンのみ）
2. 私に「使いたい技術（Node.js / Next.js など）」を伝える
3. 次に、フォルダ構成と最小コード一式を作る

必要なら次のメッセージで、**あなたのPCスペックに合わせたモデル選定**と、  
**マーダーミステリー向けのプロンプト雛形**まで作成します。
